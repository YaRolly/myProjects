export const CLASS_ELEMENT = {
    CONTAINER: 'container',
}

export const PRIORITY = {
    LOW: 'LOW',
    HIGH: 'HIGH',
};

export const STATUS = {
    TO_DO: false,
    DONE: true,
};

export const isMobileMedia = window.matchMedia('(max-width: 460px)');
export  const isDesktopMedia = window.matchMedia('(max-width: 1020px)');
